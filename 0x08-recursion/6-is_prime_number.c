#include "main.h"

int is_divisible(int num, int div);
int is_prime_numbwe(int n);
/**
 * is_divisible - checks if a number is divisible
 * @num: the number to be checked
 * @div: the divisor
 * Return: if the number is divisible
 */
int is_divisible(int num, int div)
{
	if (num % div == 0)
		return (0);
}
