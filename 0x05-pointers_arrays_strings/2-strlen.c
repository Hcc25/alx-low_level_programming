#include "main.h"
#include <string.h>
/**
 * _strlen - a function to get the length of a string
 * @s: a string pointer to passed to this function
 * Return: the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
