#include "main.h"
/**
 * main - a program that prints the alphabet in lowercase
 * Return: Always (0)
 */
int main(void)
{
	int ch;
	{
	for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
	}
putchar("\n");

	return (0);
}
